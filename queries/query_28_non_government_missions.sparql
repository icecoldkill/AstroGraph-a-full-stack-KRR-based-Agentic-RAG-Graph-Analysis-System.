PREFIX onto: <http://krr.org/space_exploration.owl#>
PREFIX ex: <http://krr.org/space/>
PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>

# Query for NonGovernmentMission (defined class: complement of GovernmentAgency missions)
SELECT ?mission ?missionName ?agency
WHERE {
    ?mission rdf:type onto:NonGovernmentMission .
    ?mission onto:missionName ?missionName .
    ?mission onto:launchedBy ?agency .
}

